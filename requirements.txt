# Main dependencies (required)
numpy>=1.21.0
scipy>=1.9.0

# Pitch shifting methods (at least one needed)
librosa>=0.10.0              # librosa_standard, librosa_hifi
soundfile>=0.12.1            # For librosa

# Optional methods (install as available)
pydub>=0.25.0                # pydub_speed
pyrubberband>=0.3.0          # pyrubberband_shift (requires rubberband-cli)
pedalboard>=0.7.0            # pedalboard_shift

# MIDI functionality
pretty_midi>=0.2.9

# Interface and utilities
tqdm>=4.64.0

# SoundTouch
soundtouch>=0.3.2